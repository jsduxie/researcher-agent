# Screenshots

Images referenced by the project README:

- `sample-email.png`: a rendered digest email. Regenerate with `python main.py --dry-run`, which prints the HTML to stdout.
- `dashboard.png`: the Streamlit review dashboard (a run with ratings, or the History page). Still outstanding.

Keep them compressed (PNG, around 1200px wide) so the repo stays small.
