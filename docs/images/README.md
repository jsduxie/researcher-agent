# Screenshots

Drop the two images referenced by the project README here:

- `dashboard.png`: the Streamlit review dashboard (a run with ratings, or the History page)
- `sample-email.png`: a rendered digest email

Keep them reasonably compressed (PNG, ~1200px wide) so the repo stays light.
